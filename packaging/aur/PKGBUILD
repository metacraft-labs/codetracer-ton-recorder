# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-ton-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-ton-recorder (CodeTracer recorder for Tolk / TON smart contract programs).

pkgname=codetracer-ton-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Tolk / TON smart contract programs"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-ton-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-ton-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-ton-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-ton-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-ton-recorder
}

check() {
  cd "$srcdir/codetracer-ton-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-ton-recorder || true
}

package() {
  cd "$srcdir/codetracer-ton-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-ton-recorder" \
    "$pkgdir/usr/bin/codetracer-ton-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
